# Tasks

Store approved Agentic Task files here.

Use:

```txt
agent-workspace/method/task-template.md
```

A Task is the unit of work for the agent.

A Task should be scoped, contextual and verifiable.

Do not create Micro Actions as separate tasks if they clearly belong inside an active Task.
