# Deprecated: Checkpoint a Run

This planning step is historical only.

The manual `agent-workspace` workflow now records checkpoints directly in the Task file.

Use `agent-workspace/planning/03-checkpoint-task.md` instead.

Current rule:

- Add checkpoint summaries to the selected Task file.
- Do not update historical Run files.
- Do not create new Run files.
