#!/usr/bin/env node
import { Command } from "commander";
import { createRequire } from "node:module";
import { assertCanonicalSetupEstablished, getWorkspaceStatus, ensureWorkspace } from "./workspace.js";
import { openRepository, type ApprovedPlan, type Task, type WorkItem } from "./repository.js";
import { discoverContext } from "./context.js";
import { nextOperation, removeActiveContext, syncActiveContext } from "./work.js";
import { advanceRefForTest, assertCleanIndex, assertExpectedHead, assertProtectedBaseline, canonicalPath, captureBaseline, createExactCommit, currentRef, gitBoundarySnapshot, headTree, publishCommit, rollbackPublishedCommit, stage, stagedTree, unstage, workFingerprint, type GitBaseline, type GitFingerprint } from "./git.js";

const packageVersion = (createRequire(import.meta.url)("../package.json") as { version: string }).version;
const program = new Command().name("nerv").description("Local-first Agent Work Harness.").version(packageVersion);
type Details = { intent: string; goal: string; scope: string; expectedTouchpoints: string; outOfScope: string; acceptanceCriteria: string; validation: string };
type Workspace = { repoRoot: string; workspaceRoot: string; databasePath: string };
type FindingSeverity = "critical" | "high" | "medium" | "low";
type ReviewFinding = { severity: FindingSeverity; finding: string; accepted_as_residual_risk?: boolean };
function action(handler: () => void) { try { handler(); } catch (error) { program.error(error instanceof Error ? error.message : String(error), { exitCode: 1, code: "NERV_OPERATION_FAILED" }); } }
function required(options: Record<string, string>, key: keyof Details) { const value = options[key]?.trim(); if (!value) throw new Error(`--${key.replace(/[A-Z]/g, (letter) => `-${letter.toLowerCase()}`)} is required.`); return value; }
function approvedPlan(value: string): ApprovedPlan { let plan: unknown; try { plan = JSON.parse(value); } catch { throw new Error("--plan must be valid JSON."); } if (!plan || typeof plan !== "object") throw new Error("--plan must be an approved Work plan object."); const input = plan as Record<string, unknown>; const workFields = ["title", "intent", "goal", "scope", "expected_touchpoints", "out_of_scope", "acceptance_criteria", "validation"] as const; const taskFields = ["title", "objective", "implementation_approach", "expected_touchpoints", "acceptance_criteria", "validation"] as const; for (const field of workFields) if (!(typeof input[field] === "string" && input[field].trim())) throw new Error(`Approved Work plan requires ${field}.`); if (!Array.isArray(input.tasks) || !input.tasks.length) throw new Error("Approved Work plan requires at least one Task."); for (const task of input.tasks) { if (!task || typeof task !== "object") throw new Error("Each approved Task must be an object."); for (const field of taskFields) if (!(typeof (task as Record<string, unknown>)[field] === "string" && String((task as Record<string, unknown>)[field]).trim())) throw new Error(`Each approved Task requires ${field}.`); } return input as unknown as ApprovedPlan; }
function reviewFindings(value?: string): ReviewFinding[] { if (!value?.trim()) return []; let parsed: unknown; try { parsed = JSON.parse(value); } catch { throw new Error("--findings must be a JSON array of severity-labeled findings."); } if (!Array.isArray(parsed) || !parsed.length) throw new Error("--findings must be a non-empty JSON array when supplied."); return parsed.map((entry) => { if (!entry || typeof entry !== "object") throw new Error("Each Review finding must be an object."); const finding = entry as Record<string, unknown>; if (!(typeof finding.finding === "string" && finding.finding.trim()) || !(["critical", "high", "medium", "low"] as string[]).includes(String(finding.severity))) throw new Error("Each Review finding requires a severity of critical, high, medium, or low and non-empty finding text."); if (finding.accepted_as_residual_risk !== undefined && typeof finding.accepted_as_residual_risk !== "boolean") throw new Error("accepted_as_residual_risk must be boolean when supplied."); if (finding.accepted_as_residual_risk && finding.severity !== "medium") throw new Error("Only medium findings may be accepted as residual risk."); return { severity: finding.severity as FindingSeverity, finding: finding.finding.trim(), ...(finding.accepted_as_residual_risk ? { accepted_as_residual_risk: true } : {}) }; }); }
function blocksPass(finding: ReviewFinding) { return finding.severity === "critical" || finding.severity === "high" || (finding.severity === "medium" && !finding.accepted_as_residual_risk); }
function formatFindings(findings: ReviewFinding[], heading: string) { return findings.length ? `${heading}:\n${findings.map((finding) => `- ${finding.severity.toUpperCase()}${finding.accepted_as_residual_risk ? " (accepted residual risk)" : ""} - ${finding.finding}`).join("\n")}` : ""; }
function recoveredFindings(value: string | null) { if (!value) return "none"; try { return formatFindings(reviewFindings(value), ""); } catch { return value; } }
function workspace(): Workspace { const status = getWorkspaceStatus(process.cwd()); if (!status.initialized || !status.repoRoot || !status.workspaceRoot || !status.databasePath) throw new Error("Nerv is not initialized in this repo. Execute `nerv init` first."); return { repoRoot: status.repoRoot, workspaceRoot: status.workspaceRoot, databasePath: status.databasePath }; }
function work(repo: any, reference: string): WorkItem { const item = repo.getWork(reference); if (!item) throw new Error(`Work Item ${reference.toUpperCase()} not found.`); return item; }
function sync(status: Workspace, item: WorkItem) { const repo = openRepository(status.databasePath); try { return syncActiveContext(status.workspaceRoot, item, repo.listTasks(item.id), repo.latestReview(item.id), repo.latestCheckpoint(item.id)); } finally { repo.close(); } }
function recommendedNext(status: Workspace, item: WorkItem) { const repo = openRepository(status.databasePath); try { return nextOperation(item, repo.listTasks(item.id), repo.latestReview(item.id)); } finally { repo.close(); } }
function taskAt(repo: any, item: WorkItem, position: string): Task { const task = repo.getTaskAt(item.id, Number(position)); if (!task || !Number.isInteger(Number(position)) || Number(position) < 1) throw new Error(`Task ${position} not found in ${item.ref}.`); return task; }
function ownedPaths(tasks: Task[]): string[] { return [...new Set(tasks.flatMap((task) => { try { return (JSON.parse(task.attribution_json ?? "{\"paths\":[]}") as { paths?: string[] }).paths ?? []; } catch { return []; } }))].sort(); }
class GitNervConsistencyError extends Error {}

program.command("init").action(() => action(() => { const status = getWorkspaceStatus(process.cwd()); if (!status.repoRoot) throw new Error("nerv init must be run inside a Git repository."); const existed = status.initialized; const result = ensureWorkspace(status.repoRoot); console.log(existed ? `Nerv is already initialized in ${status.repoRoot}.` : `Initialized Nerv in ${status.repoRoot}.`); console.log(`Repository setup: ${result.setup.filter((entry) => !entry.established).length ? `not established (${result.setup.filter((entry) => !entry.established).map((entry) => entry.path).join(", ")})` : "established at HEAD"}.`); if (result.skillSync.message) console.log(result.skillSync.message); }));
const workCommand = program.command("work").description("Deterministic Work Item persistence primitives.");
workCommand.command("materialize").requiredOption("--plan <json>").action((options: { plan: string }) => action(() => { const status = workspace(); const plan = approvedPlan(options.plan); const repo = openRepository(status.databasePath); let item: WorkItem; try { if (!(repo.listWork() as WorkItem[]).some((entry) => entry.status !== "closed")) assertCanonicalSetupEstablished(status.repoRoot); item = repo.materializePlan({ ...plan, git_baseline_json: JSON.stringify(captureBaseline(status.repoRoot)) }); } finally { repo.close(); } console.log(`Materialized ${item!.ref}: ${item!.title}\nStable ID: ${item!.id}\nContext: ${sync(status, item!)}`); console.log(`Recommended next operation: ${recommendedNext(status, item!)}`); }));
workCommand.command("materialize-rework").argument("<workRef>").requiredOption("--tasks <json>").action((reference: string, options: { tasks: string }) => action(() => { const status = workspace(); let tasks: unknown; try { tasks = JSON.parse(options.tasks); } catch { throw new Error("--tasks must be valid JSON."); } const plan = approvedPlan(JSON.stringify({ title: "approved", intent: "approved", goal: "approved", scope: "approved", expected_touchpoints: "approved", out_of_scope: "approved", acceptance_criteria: "approved", validation: "approved", tasks })); const repo = openRepository(status.databasePath); let item: WorkItem; try { item = repo.materializeRework(work(repo, reference).id, plan.tasks); } finally { repo.close(); } sync(status, item!); console.log(`Materialized remediation Tasks in ${item!.ref}.\nRecommended next operation: ${recommendedNext(status, item!)}`); }));
workCommand.command("status").argument("<workRef>").action((reference: string) => action(() => { const status = workspace(); const repo = openRepository(status.databasePath); try { const item = work(repo, reference); const tasks = repo.listTasks(item.id) as Task[]; const review = repo.latestReview(item.id); console.log(`${item.ref}: ${item.title}\nStable ID: ${item.id}\nState: ${item.status}\nTasks: ${tasks.filter((entry) => entry.status === "done").length}/${tasks.length} done\nLatest review: ${review?.outcome ?? "none"}\nRecommended next operation: ${nextOperation(item, tasks, review)}`); } finally { repo.close(); } }));
workCommand.command("list").action(() => action(() => { const status = workspace(); const repo = openRepository(status.databasePath); try { const items = repo.listWork(); console.log(items.length ? items.map((item: WorkItem) => `${item.ref}: ${item.title} [${item.status}]`).join("\n") : "No Work Items."); } finally { repo.close(); } }));
workCommand.command("show").argument("<workRef>").action((reference: string) => action(() => { const status = workspace(); const repo = openRepository(status.databasePath); try { const item = work(repo, reference); const tasks = repo.listTasks(item.id) as Task[]; const review = repo.latestReview(item.id); const checkpoint = repo.latestCheckpoint(item.id); const checkpointTask = checkpoint?.task_id ? repo.getTask(checkpoint.task_id) as Task | null : null; const details = tasks.map((entry) => `Task ${entry.position}: ${entry.title} [${entry.status}]\nObjective: ${entry.objective}\nImplementation approach: ${entry.implementation_approach}\nExpected touchpoints: ${entry.expected_touchpoints}\nAcceptance criteria: ${entry.acceptance_criteria}\nTargeted validation: ${entry.validation}\nCompletion validation evidence: ${entry.validation_evidence ?? "none"}\nAttribution: ${entry.attribution_json ?? "none"}`).join("\n\n") || "None"; const reviewDetails = review ? `ID: ${review.id}\nOutcome: ${review.outcome}\nSummary: ${review.summary}\nFindings: ${recoveredFindings(review.findings)}\nValidation evidence: ${review.validation_evidence}\nGit fingerprint: ${review.git_fingerprint_json ?? "none"}\nVerification evidence: ${review.verification_evidence ?? "none"}\nCreated at: ${review.created_at}` : "none"; const checkpointDetails = checkpoint ? `ID: ${checkpoint.id}\nTask: ${checkpointTask ? `Task ${checkpointTask.position}: ${checkpointTask.title}` : "none"}\nSummary: ${checkpoint.summary}\nNext step: ${checkpoint.next_step ?? "none"}\nCreated at: ${checkpoint.created_at}` : "none"; console.log(`${item.ref}: ${item.title}\nStable ID: ${item.id}\nState: ${item.status}\nIntent: ${item.intent}\nGoal: ${item.goal}\nScope: ${item.scope}\nExpected touchpoints: ${item.expected_touchpoints}\nOut of scope: ${item.out_of_scope}\nAcceptance criteria: ${item.acceptance_criteria}\nFull validation: ${item.validation}\n\nTasks:\n${details}\n\nLatest review:\n${reviewDetails}\nLatest checkpoint:\n${checkpointDetails}`); } finally { repo.close(); } }));
const task = workCommand.command("task").description("Task operations scoped to a Work Item.");
task.command("start").argument("<workRef>").argument("<position>").action((reference: string, position: string) => action(() => transitionTask(reference, position)));
task.command("done").argument("<workRef>").argument("<position>").requiredOption("--evidence <evidence>").requiredOption("--files <paths...>").action((reference: string, position: string, options: { evidence: string; files: string[] }) => action(() => completeTask(reference, position, options)));
program.command("review").argument("<workRef>").requiredOption("--outcome <PASS|REWORK>").requiredOption("--summary <summary>").requiredOption("--validation-evidence <evidence>").option("--verification-evidence <evidence>").option("--findings <json>").option("--remediation-title <title>").option("--remediation-objective <objective>").option("--remediation-approach <approach>").option("--remediation-touchpoints <touchpoints>").option("--remediation-acceptance-criteria <criteria>").option("--remediation-validation <validation>").action((reference: string, options: { outcome: string; summary: string; validationEvidence: string; verificationEvidence?: string; findings?: string; remediationTitle?: string; remediationObjective?: string; remediationApproach?: string; remediationTouchpoints?: string; remediationAcceptanceCriteria?: string; remediationValidation?: string }) => action(() => reviewWork(reference, options)));
program.command("checkpoint").argument("<workRef>").requiredOption("--summary <summary>").option("--task <position>").option("--next-step <step>").action((reference: string, options: { summary: string; task?: string; nextStep?: string }) => action(() => { const status = workspace(); const repo = openRepository(status.databasePath); let item: WorkItem; try { item = work(repo, reference); const entry = options.task ? taskAt(repo, item, options.task) : null; repo.createCheckpoint({ work_item_id: item.id, task_id: entry?.id ?? null, summary: options.summary, next_step: options.nextStep ?? null }); } finally { repo.close(); } sync(status, item!); console.log(`Checkpoint saved for ${item!.ref}.\nRecommended next operation: ${recommendedNext(status, item!)}`); }));
program.command("close").argument("<workRef>").requiredOption("--message <message>").action((reference: string, options: { message: string }) => action(() => { closeWork(reference, options.message); console.log("Recommended next operation: No further Nerv lifecycle operation is required."); }));
program.command("status").action(() => action(() => { const status = getWorkspaceStatus(process.cwd()); console.log(`Nerv status: ${status.initialized ? "initialized" : "not initialized"}`); if (status.initialized && status.repoRoot) { const context = discoverContext(status.repoRoot); console.log(`Product Context: ${context.product ? "available" : "not available"}`); console.log(`Repo Context: ${context.repo ? "available" : "not available"}`); } }));
function transitionTask(reference: string, position: string) { const status = workspace(); const repo = openRepository(status.databasePath); let item: WorkItem; let entry: Task; try { item = work(repo, reference); entry = repo.startTask(item.id, Number(position)); item = work(repo, item.id); } finally { repo.close(); } sync(status, item!); console.log(`Started Task ${entry!.position} in ${item!.ref}.\nRecommended next operation: ${recommendedNext(status, item!)}`); }
function completeTask(reference: string, position: string, options: { evidence: string; files: string[] }) { const status = workspace(); const repo = openRepository(status.databasePath); let item: WorkItem; let entry: Task; try { item = work(repo, reference); entry = taskAt(repo, item, position); const baseline = JSON.parse(item.git_baseline_json ?? "null") as GitBaseline | null; if (!baseline) throw new Error("Task completion requires an activation Git baseline."); assertExpectedHead(status.repoRoot, baseline.head); const paths = [...new Set(options.files.map((path) => canonicalPath(status.repoRoot, path)))].sort(); if (paths.some((path) => baseline.protected_paths.includes(path))) throw new Error("Baseline-dirty paths cannot be attributed to a Task."); entry = repo.completeTask(entry.id, options.evidence.trim(), JSON.stringify({ paths })); item = work(repo, item.id); } finally { repo.close(); } sync(status, item!); console.log(`Completed Task ${entry!.position} in ${item!.ref}.\nRecommended next operation: ${recommendedNext(status, item!)}`); }
function reviewWork(reference: string, options: { outcome: string; summary: string; validationEvidence: string; verificationEvidence?: string; findings?: string; remediationTitle?: string; remediationObjective?: string; remediationApproach?: string; remediationTouchpoints?: string; remediationAcceptanceCriteria?: string; remediationValidation?: string }) { const status = workspace(); const repo = openRepository(status.databasePath); let item: WorkItem; let outcome: "PASS" | "REWORK"; let findings: ReviewFinding[]; try { item = work(repo, reference); outcome = options.outcome.toUpperCase() as "PASS" | "REWORK"; if (!(["PASS", "REWORK"] as string[]).includes(outcome)) throw new Error("Review outcome must be PASS or REWORK."); findings = reviewFindings(options.findings); const blockers = findings.filter(blocksPass); const remediation = [options.remediationTitle, options.remediationObjective, options.remediationApproach, options.remediationAcceptanceCriteria, options.remediationValidation]; const verificationEvidence = options.verificationEvidence?.trim() ?? null; if (outcome === "REWORK" && (!blockers.length || remediation.some((value) => !value?.trim()))) throw new Error("REWORK requires blocking findings and an execution-ready remediation Task."); if (outcome === "PASS" && blockers.length) throw new Error("PASS is not permitted while critical, high, or unaccepted medium findings remain."); if (item.status === "review" && !(outcome === "REWORK" && verificationEvidence)) throw new Error("Only REWORK with verification evidence may replace a PASS review."); if (item.status !== "active" && item.status !== "review") throw new Error("Work Review requires an active Work Item with all Tasks done."); const baseline = JSON.parse(item.git_baseline_json ?? "null") as GitBaseline | null; if (!baseline) throw new Error("Work Review requires an activation Git baseline."); assertCleanIndex(status.repoRoot); assertExpectedHead(status.repoRoot, baseline.head); assertProtectedBaseline(status.repoRoot, baseline); const current = workFingerprint(status.repoRoot, baseline, ownedPaths(repo.listTasks(item.id) as Task[])); if (item.status === "review") { const pass = repo.latestReview(item.id); if (!pass?.git_fingerprint_json || JSON.stringify(current) !== pass.git_fingerprint_json) throw new Error("Git state changed after PASS; verification REWORK requires the reviewed state."); } const fingerprint = outcome === "PASS" ? current : null; repo.createReview({ work_item_id: item.id, outcome, summary: options.summary.trim(), findings: findings.length ? JSON.stringify(findings) : null, validation_evidence: options.validationEvidence.trim(), git_fingerprint_json: fingerprint ? JSON.stringify(fingerprint) : null, verification_evidence: verificationEvidence }); item = work(repo, item.id); } finally { repo.close(); } sync(status, item!); console.log(`${outcome === "PASS" ? `PASS recorded for ${item!.ref}. Optional user or external verification may proceed before Close.` : `REWORK recorded for ${item!.ref}.\n${formatFindings(findings!, "Blocking findings")}\n\nProposed remediation Task:\nTitle: ${options.remediationTitle}\nObjective: ${options.remediationObjective}\nImplementation approach: ${options.remediationApproach}\nExpected touchpoints: ${options.remediationTouchpoints ?? "none"}\nAcceptance criteria: ${options.remediationAcceptanceCriteria}\nTargeted validation: ${options.remediationValidation}`}${outcome === "PASS" && findings!.length ? `\n${formatFindings(findings!, "Residual findings")}\nResidual findings do not block Close.` : ""}\nRecommended next operation: ${recommendedNext(status, item!)}`); }
function closeWork(reference: string, message: string) { const status = workspace(); const repo = openRepository(status.databasePath); try { const item = work(repo, reference); const review = repo.latestReview(item.id); if (item.status !== "review" || review?.outcome !== "PASS" || !review.git_fingerprint_json || !item.validation_evidence) throw new Error(`Work Item ${item.ref} is not ready to close.`); const baseline = JSON.parse(item.git_baseline_json ?? "null") as GitBaseline | null; if (!baseline || (repo.listTasks(item.id) as Task[]).some((entry) => entry.status !== "done")) throw new Error("Close requires an activated Work Item with all Tasks done."); const fingerprint = JSON.parse(review.git_fingerprint_json) as GitFingerprint; assertCleanIndex(status.repoRoot); assertExpectedHead(status.repoRoot, baseline.head); assertProtectedBaseline(status.repoRoot, baseline); if (JSON.stringify(workFingerprint(status.repoRoot, baseline, ownedPaths(repo.listTasks(item.id) as Task[]))) !== JSON.stringify(fingerprint)) throw new Error("Git state changed after PASS; Work cannot close as reviewed."); if (fingerprint.tree === headTree(status.repoRoot, baseline.head)) { repo.closeWork(item.id, null); removeActiveContext(status.workspaceRoot, item.ref); console.log(`Closed ${item.ref}: no tracked Git diff.`); return; } let pending: ReturnType<typeof createExactCommit> | null = null; try { stage(status.repoRoot, fingerprint.paths); if (stagedTree(status.repoRoot) !== fingerprint.tree) throw new Error("Staged Git tree does not match the PASS-reviewed tree."); pending = createExactCommit(status.repoRoot, fingerprint.tree, baseline.head, `${message}\n\nNerv-Work: ${item.id}\nNerv-Work-Ref: ${item.ref}`); publishCommit(status.repoRoot, pending, baseline.head); if (process.env.NERV_TEST_EXTERNAL_REF_ADVANCE) { advanceRefForTest(status.repoRoot, pending.ref, pending.commit); if (process.env.NERV_TEST_CAPTURE_BOUNDARY) process.stderr.write("NERV_TEST_BOUNDARY:" + gitBoundarySnapshot(status.repoRoot, pending.ref) + "\n"); } try { if (process.env.NERV_TEST_FAIL_CLOSE_PERSISTENCE) throw new Error("simulated durable Close persistence failure"); repo.closeWork(item.id, pending.commit); } catch (error) { try { if (process.env.NERV_TEST_FAIL_COMPENSATION) throw new Error("simulated compensation CAS failure"); rollbackPublishedCommit(status.repoRoot, pending, baseline.head); } catch { throw new GitNervConsistencyError(`Git/Nerv consistency failure after Close persistence error: activation ${baseline.head}, Nerv commit ${pending.commit}, observed ${currentRef(status.repoRoot, pending.ref)}.`); } throw error; } removeActiveContext(status.workspaceRoot, item.ref); console.log(`Closed ${item.ref}: ${pending.commit}`); } catch (error) { if (!(error instanceof GitNervConsistencyError)) unstage(status.repoRoot, baseline.head, fingerprint.paths); throw error; } } finally { repo.close(); } }
await program.parseAsync();
